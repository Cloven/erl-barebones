{application, barebones, [
	{description, ""},
	{vsn, "0.1.0"},
	{id, "git"},
	{modules, []},
	{registered, []},
	{applications, [
		kernel,
                observer,
		stdlib,
                compiler,
                syntax_tools,
                goldrush,
                lager
	]},
	{mod, {barebones, []}},
	{env, [
          ]}
]}.
